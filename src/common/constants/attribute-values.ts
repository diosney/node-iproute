export enum OnOffToggle {
  On  = 'on',
  Off = 'off'
}

export enum EnableDisableToggle {
  Enable  = 1,
  Disable = 0
}

export enum EnableDisableAutoToggle {
  Enable  = 'enable',
  Disable = 'disable',
  Auto    = 'auto'
}