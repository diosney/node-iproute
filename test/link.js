var assert = require('assert');

describe('ip link', function () {
	var ip_link = require('../lib/link');

	describe('#show()', function () {
		it('should return -1 when the value is not present');
	});
});